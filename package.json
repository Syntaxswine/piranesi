{
  "name": "carceri",
  "version": "0.1.0",
  "private": true,
  "description": "A block-building game drawn as a copperplate etching, after Piranesi's Carceri d'Invenzione.",
  "type": "module",
  "license": "UNLICENSED",
  "engines": { "node": ">=22" },
  "scripts": {
    "serve": "node tools/serve.mjs --port 8749",
    "serve:shots": "node tools/serve.mjs --port 8749 --shots",
    "test": "node --test \"test/*.test.mjs\"",
    "shot": "node tools/plateshot.mjs",
    "check": "npm run test && node tools/plateshot.mjs --scene probe --out docs/shots/probe.png --stats"
  },
  "//": "No dependencies, and there will not be any. Node built-ins only, and only in tools/ and test/. The renderer itself never touches a canvas — see tools/png.mjs on why."
}
